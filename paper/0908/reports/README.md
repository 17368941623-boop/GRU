# Verification outputs

`preflight_report.json` is the package-level audit. `smoke_training/` and `graph_variant_smoke/` are deliberately
small interface tests, not formal experiments and not paper results. Formal runs must be written to `outputs/`.

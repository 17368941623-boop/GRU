# PyTorch must be installed by conda with the CUDA version appropriate for the server.
# This file intentionally does not install or replace torch.
joblib>=1.3
numpy>=1.26
pandas>=2.0
scipy>=1.11
statsmodels>=0.14
tigramite>=5.2

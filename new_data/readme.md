## 论文结构说明
此文件用于指引整体的论文方向，你在读取的时候可以作为参考
### 更新时间2026.9.12

## 论文结构
一 、总体的概述
1、 并行结构的引入（待确认） 
RNN-LSTM-GRU的发展，可以提取历史趋势，但是....(在我这边的不足是什么)
引入并行结构（哪些论文进行了哪些的研究）
GNN可以确定数据的流向（以及其他的优势），
未来的阀门控制序列，对于直接在带阀门的数据上预测未来趋势，不给未来的阀门序列是不对的，需要同时给未来阀门的控制序列，引出并行结构

2、PCMCI的优势 
PCMCI的发展，各自的优缺点，将PCMCI和GNN的结合（可以有什么优势）
GNN的优势是可以确定历史信息的流向 ，但是对于流程工业，中间过程的阀门是人为的控制因素 ，阀门数据不可以作为被指向的对象，因此不可以作为静态物理图直接送入模型 



二、我的模型架构
1、我目前方案的总体介绍

2、GRU-lookback-hiddensize的联合测试（p1广范围hotmap-p2细范围hotmap）     
目标：选择验证性能落在最优值一个标准误差以内的最佳lookback-hiddensize组合
简要论证：lookback和hiddensize各自大小的对模型的影响，一个模型的主要影响因素为 lookback 和 hiddensize，其余特征构造、预测步长等部分，在后续论文中讨论。

3、PCMCI部分，静态物理图的获取 --静态物理图（p3）
PCMCI的计算部分 ，分为水平level分析和差分difference分析（解释为什么这样分）
分出来的结果是什么？
温度、压力、流量：主要使用一阶变化量，阀门：同时保留开度水平u(t)和开度变化Δu(t)，Δ阀门 → Δ温度适合检测阀门动作后的即时响应，阀门保持在某一开度时，绝对开度仍决定流通能力，不能只保留差分。

三、理论证明
补充

三、实验论证
1、消融实验部分
| 模型 | 要回答的问题 |
|---|---|
| Raw54-GRU | 当前强基线，已完成 |
| PCMCI-Selected-GRU | PCMCI筛选特征本身是否有效 |
| Static-GNN+GRU | 仅静态物理图是否有效 |
| PCMCI-GNN+GRU | 纯数据图是否有效 |
| Static+PCMCI+GNN+MLP+GRU | 不使用KAN时的效果 |
| Static+PCMCI+GNN+KAN+GRU | 最终完整模型 |
| Full-ShuffledLag | 正确时延是否真的有价值 |
| Full-RandomGraph | 图拓扑是否优于同规模随机图 |
| Full-No-Hmix | 虚拟汇合节点是否优于阀门直接连接Thv |
| Full-No-BranchKAN | 上游三支路KAN门控是否必要 |


## 实际完成汇总 

## 过去的工作汇总
/0822文件夹 主数据管线